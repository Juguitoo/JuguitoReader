# Archive — v1.2.0

Estabilización (audit) + features de la línea 1.2. Build orientado a **pruebas cerradas** (Play). READER-008 / READER-009 diferidos a v1.4.0.

## Features / tareas

| ID | Tarea | Tipo | Commit / nota |
|----|-------|------|---------------|
| TAR-25 | Cálculo de velocidad de lectura | Feat | JS bridge → sesiones |
| TAR-27 | Registro de sesiones de lectura por libro | Feat | |
| TAR-28 | Idiomas (i18n) | Feat | |
| TAR-44 | Automatizaciones Pendiente → Leyendo / 100% → Finalizado | Feat | |
| TAR-45 | Sugerencia cambio de estado al abrir libro | Feat | |
| TAR-46 | Porcentaje en portadas (libros leyendo) | Feat | |
| TAR-55 | Ajuste tema neon (contrastes) | Feat | |
| ARCH-001 | Eliminar scaffold sync (SyncStatus, deps Supabase) | Clean | `c4aaa81` (CLEAN-001) |
| CLEAN-002 | SortOption.displayName → strings.xml | Clean | |
| DATA-009 | Separar INSERT/UPDATE en ReadingProgressDAO | Clean | Higiene; REPLACE no rompía FK |
| I18N-001 | Strings hardcodeadas en EpubParser | Fix | `8347a76` |
| GIT-001 | Rama v1.2.0 → eliminada / gitflow | Chore | GIT_WORKFLOW |
| DOC-001 | Documentación base (README, docs/, rules) | Docs | |
| REL-003 | CI: `./gradlew test` en push (GitHub Actions) | Chore | Hecho en esta línea |

## Issues resueltos

Orden aproximado de introducción (audit → cierre). Columna Comentario = fix en una frase; detalle largo solo bajo la tabla si aporta dominio.

| ID | Título | Sev. | Commit | Comentario |
|----|--------|------|--------|------------|
| DATA-001 | `REPLACE` en updates rompe FK CASCADE | Crítico | `3b6823c`, `ed6a26d`, `41257b4`, `28a8981` | Separar `@Insert` / `@Update` en Book, Folder, Genre. Cierra también READER-003. |
| DATA-002 | Cross-refs no se sincronizan al editar | Crítico | `06fe93b` | Transacción DELETE + INSERT en junction tables. |
| READER-003 | Progress update usaba saveBook(REPLACE) | Crítico | `41257b4` | Vía DATA-001: usa `updateBook`. |
| READER-001 | Typo `windows.scrollY` en JS | Alto | `737d185` | Corregido a `window.scrollY`; progreso/WPM dejaban de reportarse. |
| READER-002 | PDF aceptado pero no soportado | Alto | `6ac4c4a` | Quitar PDF de mime types del picker hasta haber motor. |
| DATA-003 | Relaciones resueltas por nombre | Alto | `901ad24`, `d26e95c` | Usar `id` cuando `id != 0` tras renombrar carpeta/género. |
| FILE-001 | Mezcla content:// URI vs path filesystem | Alto | `906b659`, `32e8d83` | Paths internos coherentes; no pasar URI cruda a `File(path)`. |
| FILE-002 | Reemplazar EPUB guarda path incorrecto | Alto | `8cf2a86`, `32e8d83` | Draft guarda path interno tras copy, no el URI del picker. |
| FILE-003 | Parse EPUB en hilo Main | Alto | `7f4cf25`, `1bbe4fb` | `withContext(Dispatchers.IO)` en parse/import. |
| SEC-001 | WebView file:// + allowFileAccess | Alto | `903dede`, `6822276` | WebViewAssetLoader; sin file://. Residual: JS bridge necesario. |
| SEC-002 | Zip bomb en EpubParser | Alto | `11e1d4d`, `14078f0` | Límites de bytes/entries; abort + cleanup. |
| SEC-003 | Path traversal tras unzip | Alto | `9f9ef29`, `1fb4e4c` | Resolución canónica; fail-closed. |
| READER-004 | Crash al reemplazar EPUB (index OOB) | Alto | `fbf09aa`, `44061a0`, `96b565d` | Reset transaccional de progreso/sesiones + cleanup caché. Residual: Room≠filesystem. |
| FILE-004 | Archivos internos huérfanos | Alto | `f575581`, `a302657`, `3bb1edf`, `092c7b1`, `e76f776` | Commit on save (staging → promote); rollback si falla Room. |
| DATA-007 | Undo delete no restaura daily_reading | Alto | — | Snapshot + restore transaccional (`RestoreDeletedBookUseCase`). |
| DATA-008 | Add/UpdateBook sin transacción atómica | Alto | `fe1e2ee` | `insert/updateBookWithCrossRefs` en una transacción. Residual: resolución de carpetas fuera. |
| DATA-004 | reading_progress huérfano al borrar | Medio | — | FK CASCADE 10→11; undo incluye progreso. |
| READER-005 | Brillo no se restaura al salir | Medio | — | Restore `BRIGHTNESS_OVERRIDE_NONE` en onDispose. |
| READER-006 | System bars ocultas al salir | Medio | — | `show(systemBars())` en onDispose. |
| READER-007 | WebView no destruido | Medio | — | `stopLoading` / remove bridge / `destroy` en onRelease. |
| READER-010 | Errores unzip silenciados | Medio | `6d21d74` | Fail-fast parse + cleanup caché en extracción. |
| FILE-005 | openInputStream null → path fantasma | Medio | `deca14f` | Throw si stream null antes de crear fichero. |
| FILE-006 | Rollback de promote borra EPUB/cover ya internos | Alto | `4c47a6e` | Detail solo pasa al promote paths distintos del libro; `onSuccess` no borra persistidos si `filesResult` es null. |
| FILE-007 | Cover en `cacheDir` no se promueve al guardar | Alto | `07768ec` | `promotePendingFiles` copia cover de cache a `filesDir`; Add Book import EPUB. |
| FILE-008 | Cover huérfana si falla el copy del EPUB | Medio | `15020a4` | `persistFiles=true`: si peta el copy del EPUB, borrar la cover ya escrita en `filesDir`. |
| UX-003 | Library abre el lector en físico / sin EPUB | Alto | `d9acaa2` | Tap: detalle si físico o sin path; badge Físico. |
| UX-004 | Snackbar de carpeta muestra la clave cruda | Alto | `9555afe` | `snackbar_result` pasa `R.string`; create vs update. |
| UX-018 | Home “estantería vacía” con solo físicos / sin EPUB | Medio | `f1a3128` | Success con shelves vacíos muestra empty propio, no “biblioteca vacía”. |
| UX-025 | Flag físico no oculta la UI de EPUB | Bajo | `ca9a057` | Add/Detail ocultan picker EPUB; al guardar físico se fuerza path null. |
| FILE-017 | Digital sin fichero / físico con EPUB | Medio | `ca9a057` | Físico no conserva EPUB. Digital sin fichero permitido (producto). |
| FILE-009 | Undo Home de un solo hueco | Alto | — | `confirmPending` al nuevo delete; `pendingUndoBookId` + `LaunchedEffect` (no snackbar one-shot). |
| UX-001 | Loading global al importar | Medio | `6c5f9f8` | Flag `isImporting` + overlay; no `UiState.Loading` global. |
| UX-002 | Stuck en Loading tras error | Medio | `6c5f9f8` | `try/finally` resetea loading en import/save. |
| PERF-001 | Escrituras excesivas en scroll | Medio | `e815a1d` | Debounce 2 s + flush en pause/back/capítulo. |
| READER-011 | Conteo palabras al retroceder | Medio | `fe50fe3` | Reset de acumulado al guardar sesión; ver nota abajo. |
| READER-012 | WebView sin onRenderProcessGone | Medio | `2136d05` | Recrear WebView; Error tras N reintentos. |
| READER-013 | FOUC al cambiar de capítulo | Medio | `b63d0c3` | WebView invisible hasta CSS/tema aplicados. |
| READER-014 | Timer de sesión durante Loading | Medio | `a89bce2` | Start/finish reading antes del guard de Success. |
| READER-015 | Palabras regaladas al restaurar scroll | Medio | `fe50fe3` | Baseline `reportInitialWordsRead` sin acreditar. |
| ARCH-002 | Auto Backup vs local-only | Medio | `853a3a4` | `allowBackup=false` + excludes D2D. Gate backup manual: TAR-59. |
| I18N-001 | Strings hardcodeadas EpubParser | Mejora | `8347a76` | `R.string.epub_chapter_title` ES/EN. |
| DATA-013 | BookDetail delete: respetar `Result.failure` | Medio | `861fb2a` | `result.fold` en vez de `runCatching`; ficheros y pop solo en success. |
| UX-005 | Error de carga en Registry parece lista vacía | Medio | `d594439` | `ErrorView` + `OnDismissError`; el collect de éxito limpia `errorMessage`. |
| UX-007 | Snackbar de import EPUB hardcodeado en ES | Medio | `2415df2` | `StringResource(R.string.import_epub_successfull)`. |
| UX-009 | Orden de saga rechaza decimales | Medio | `acd4346` | Regex `99.99` + `KeyboardType.Decimal`; coma → punto. |
| UX-010 | Temas del lector en Settings en inglés | Medio | `efdc28d` | `ReaderTheme.toUiText()` vía `strings.xml`. |
| UX-019 | Sheet de carpetas muerta en Library Empty | Bajo | `81cf60b` | Se oculta `FolderSelectorRow` en Empty. |
| UX-021 | Libro inexistente: error engañoso / hardcode ES | Medio | `4450260` | Null → `error_book_not_found`; fallo → `error_loading_data`. |
| FILE-010 | Confirm de delete en Home no borra `cache/reader` | Bajo | `2aae12c` | `onConfirm` también llama `deleteReaderCache`. |
| FILE-013 | Back en edit deja ficheros de staging | Bajo | `79b501f` | `OnDiscard` + `BackHandler` → cleanup y `NavigateBack`. |
| FILE-012 | Staging de cámara no se limpia | Bajo | `80f3c05` | Draft guarda `absolutePath` de cache; galería sigue `content://`. Cancel borra el jpeg; Add/Detail limpian staging al cambiar de portada. |
| READER-021 | ETA del lector fija a 250 WPM | Bajo | `ab94f46` | Media ponderada de `daily_reading` del libro si hay ≥5 min; si no, 250. Recalcula al cargar capítulo; no usa la sesión en vivo. |
| DATA-014 | Overflow `Int` al fusionar WPM diario | Bajo | `e5c7b31` | Productos `readingSpeed * timeSpentMillis` en `Long` antes de la media ponderada. |
| DATA-016 | `getAllGenreNames()` no era `suspend` | Bajo | `487c5cc` | Query `suspend`; Room la ejecuta fuera de Main. |
| FILE-011 | `takePersistableUriPermission` sin catch | Alto | `68d477c` | Quitado: tras copiar a `filesDir` el grant persistente no se usa; algunos providers crasheaban. |
| DATA-011 | Add/update dejan el draft expuesto y pueden resucitar carpetas | Medio | — | Drawer bloqueado en add/update; si el id del draft ya no existe, se omite la relación (no `insert` por nombre). |
| UX-008 | Columna Inicio del Registry ordena por `createdAt` | Medio | `d797472` | Comparador y enum `STARTED_AT_*` usan `startDate`, la fecha de la celda. |
| UX-015 | Delete en Home como side-effect de composición | Medio | `3955a48` | `OnDeleteBookClick` sale en `LaunchedEffect(bookToDelete)`, no en el cuerpo del Composable. |
| UX-017 | Registry `LazyColumn` sin `key` | Medio | — | `items(filteredBooks, key = { it.id })`: el estado del comentario viaja con el libro al ordenar. |

### Notas de dominio

**READER-011 / WPM:** las palabras alimentan el WPM de la sesión. Retroceder scroll no suma; **releer sí cuenta** (lectura real con tiempo de sesión). El bug real era que `saveCurrentReadingSession` reseteaba `accumulatedReadWords` y una escritura posterior con estado stale lo revertía. Fix: escrituras posteriores vía estado fresco + `creditWordsRead` con `delta.coerceAtLeast(0)`.

**Límites arquitecturales recurrentes:** Room y filesystem no comparten transacción (READER-004, FILE-004, DATA-008). Huérfanos de disco previos a v1.2.0 no se barrieron.
